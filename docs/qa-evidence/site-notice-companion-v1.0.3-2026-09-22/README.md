# kakao-site-notices 1.0.3 오픈채팅 호환 후보

상태: 로컬 후보, Git tag/게시/서버 배포 없음. 사용자 제공 로그로 1.0.3 실기기 버전·진단 응답 확인. 수신 등록·실제 충원 수신 미확인.

## 실기기 원인 근거

사용자 제공 1.0.2 로그: 2026-09-22 09:25:44 INIT_READY, 09:26:26/27 CALLBACK_RECEIVED와 GROUP_REQUIRED. 사용자가 실제 내전 구인 오픈채팅방에서 보낸 명령이라고 확인했다. 그룹 표시 값이 falsey인 경우 조기 반환하는 코드가 무응답을 발생시켰다. Android SDK가 왜 이 값을 주었는지는 아직 확인하지 않았다.

## 변경과 검증

- response/handleMessage/register의 그룹 표시 필수 조건을 제거했다. 카카오톡 패키지·명시적 일회용 코드·서버 승인·Replier 대상 고정과 중복 억제는 유지한다. 개인방/그룹방 자동 구분은 하지 않으며 원하는 오픈채팅에서 직접 등록한다.
- 진단은 실제 그룹 확인이라고 표시하지 않고 그룹 표시 꺼짐/미제공(호환 처리)을 표시한다. GROUP_FLAG_COMPAT은 정상 호환 처리 로그다.
- `node --test tests/kakao-site-notice-phone.test.mjs`: 18 PASS. false/undefined/null/0 그룹 표시의 버전·진단·상태 응답, 잘못된 코드 무등록, 정확한 코드 등록·폴링, 다른 세션으로 대상 변경 불가, 중복 억제, 다른 앱/패키지 누락 차단 포함.
- Rhino ES5 정적 검사: 경고 후보 0. 실제 Android 엔진 실행과 구분한다.
- `npm run check`: exit 0, lint·타입·ERD·전체 계약/단위·이미지 검증·빌드 통과. 로그 `.tmp/site-notice-1.0.3-check.log`. 현재 트리 비밀 검사와 `git diff --check` 통과.
- 새 개인 설치본 VM 검증 2 PASS: 그룹 false에서 정상/설정 초기화 실패 모두 버전과 진단 응답. 운영 HTTP 또는 실제 메시지 전송 없음.
- 개인 설치본: 13,112자 / 13,911바이트 / SHA-256 `d61e48af666ae749ca859ef6e76d0466100a67ec44a343cb98dbce88e06301e2`. 서버 target ID/hash 유지, 로컬 등록 코드 신규 발급, 이전 파일 비공개 백업 유지.

## 다음 확인

2026-09-22 09:32~09:33 사용자 제공 실기기 응답에서 KLOL_SITE_NOTICE_COMPANION_1.0.3, 메시지 수신 정상, 카카오톡 확인, 그룹 표시 꺼짐/미제공(호환 처리), 초기화 READY를 확인했다. 이 응답은 로컬 진단으로 서버 연결·충원 발송을 실행하지 않는다. 실제 설치 파일의 해시를 휴대폰에서 추출한 증거는 아니며 버전 응답 확인으로 기록한다.

동일한 실제 내전 구인 오픈채팅방에서 새 개인 안내의 일회용 코드로 등록한 뒤 SITE 9→10 충원 수신을 확인한다. 운영 서버·DB·기존 R25 변경 없음.
