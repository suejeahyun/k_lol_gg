# 디스코드 복붙용 패치 공지 — V2 데이터 기반

```text
[K-LOL.GG V2 개발 현황]

V2 전용 PostgreSQL 데이터 기반을 독립 코드에 통합했습니다.

• 전진 migration과 안전한 DB 연결·transaction 구조 추가
• 관리자 계정·세션·TOTP·로그인 제한용 저장소 계약 추가
• 플레이어 공개 검색 projection과 감사 로그 기반 추가
• 실제 PostgreSQL 18 격리 테스트 8/8 통과
• 전체 lint·타입 검사·테스트·production build 통과
• 관리자 인증 HTTP 검증 통과
• 런타임 의존성 취약점 0건 확인

아직 운영 서버나 운영 DB에는 반영하지 않았습니다. 운영형 DB 세션·TOTP 키 관리와 전체 사용자/관리자 기능 동등성이 완료되기 전에는 V2를 운영으로 전환하지 않습니다.
```

## 다음 패치 추천

1. DB 기반 관리자 세션 발급·재검증·로그아웃 폐기
2. 분산 로그인 제한과 TOTP 암호화 keyring 연결
3. 플레이어 사용자 검색·상세와 관리자 CRUD의 동일 도메인 계약 완성
4. ADMIN/SUPER 합성 계정 기반 전체 관리자 경로 E2E
5. 기능 슬라이스별 복구·감사·권한 실패 계약 자동화
