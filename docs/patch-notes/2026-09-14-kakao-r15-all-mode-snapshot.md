# 카카오 V1 strict R15 내전 전체 양식 무응답 수정

증바람·칼바람처럼 `게임정보`와 `주최자`가 포함된 완성 내전 양식을 전송했을 때 봇이 답하지 않던 문제를 수정했다.

- 완성 내전 양식을 파티 양식으로 먼저 오인하던 휴대폰 라우팅 순서를 보정
- 내전 양식 외형이 확인된 경우 `FEATURES` 게이트웨이로 원문을 한 번만 전달
- 참가자가 있는 완성 양식과 10개 빈 참가행을 갖춘 취소용 전체 양식을 모두 지원
- 잘렸거나 참가행이 부족한 양식, 일반 대화, 실제 파티 양식은 기존처럼 오전송하지 않음
- 서버의 협곡·칼바람·증바람 파싱과 기존 V1 표시 형식은 변경하지 않음

휴대폰 설치본 버전은 `KLOL_KAKAO_BOT_V40_SITE_FIRST_NO_CODES_R15_2026_09_14_ALL_MODE_SNAPSHOT`이다. `.private/KLOL_KAKAO_BOT_R15_ALL_MODE_SNAPSHOT_REPLACE_ALL.js` 전체를 기존 코드와 교체해야 한다.

DB migration과 운영 데이터 직접 수정·삭제는 없다.
